# 仕様書：PhotoViewer

## 1. 役割・目的

指定された写真データ (`latestPhoto`) を画面中央に描画し、撮影日時をフォーマットして表示する純粋なUIコンポーネント。

## 2. Props

- `latestPhoto`: 表示すべき1枚の写真データオブジェクト（`null` の場合は写真なし表示）。
- `loading`: データ取得中かどうかを示すフラグ（`true` の場合はローディング表示）。
